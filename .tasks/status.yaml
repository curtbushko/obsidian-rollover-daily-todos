project: "mark-forwarded-on-rollover"
phase_id: 3
phase_name: "Plugin Marking Logic"
phase_file: "phase-03-plugin-marking-logic.md"
execution_order: [1, 2, 3, 4]
tasks:
  - id: 1
    name: "Create marking utility function with tests"
    status: completed
    deps: []
    plan_tasks:
      - "Implement marking logic in rollover(): when `markForwardedOnRollover` is true, replace `[ ]` with `[>]` in yesterday's note for rolled-over todos"
  - id: 2
    name: "Handle child todos in marking logic"
    status: completed
    deps: [1]
    plan_tasks:
      - "Handle child todos: when `rolloverChildren` is enabled, mark child checkbox items with `[>]` but leave plain text unchanged"
  - id: 3
    name: "Integrate marking into rollover() method"
    status: completed
    deps: [2]
    plan_tasks:
      - "Write tests for marking logic: basic marking, child handling, interaction with `deleteOnComplete`"
  - id: 4
    name: "Verify undo functionality with marking"
    status: completed
    deps: [3]
    plan_tasks:
      - "Verify undo functionality works correctly (existing mechanism should handle it)"
